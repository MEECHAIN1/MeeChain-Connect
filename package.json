{
  "name": "webapp",
  "version": "1.0.0",
  "description": "แดชบอร์ด Web Application สำหรับ MeeChain Blockchain Platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p deploy/public/src/css deploy/public/src/js deploy/public/src/web3 deploy/public/src/assets/images deploy/functions/api/chat deploy/functions/api/nodecloud deploy/functions/api/web3 && cp -f *.html deploy/public/ && cp -f manifest.json deploy/public/ 2>/dev/null || true && cp -f sw.js deploy/public/ 2>/dev/null || true && cp -f generated-icon.png deploy/public/ 2>/dev/null || true && cp -f _headers deploy/public/ 2>/dev/null || true && cp -f _redirects deploy/public/ 2>/dev/null || true && cp -r src/css/. deploy/public/src/css/ && cp -r src/js/. deploy/public/src/js/ && cp -r src/web3/. deploy/public/src/web3/ && cp -r src/assets/images/. deploy/public/src/assets/images/ && cp -r functions/. deploy/functions/",
    "test": "echo 'No tests'",
    "check:deploy": "npm run build && node scripts/check-deploy.mjs"
  },
  "keywords": [
    "MeeChain",
    "Blockchain",
    "Dashboard"
  ],
  "author": "Thanawat",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^16.4.5",
    "ethers": "^6.16.0",
    "express": "^4.18.2",
    "js-yaml": "^4.1.1",
    "openai": "^6.25.0",
    "ws": "^8.19.0"
  }
}
